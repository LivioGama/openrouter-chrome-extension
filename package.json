{
  "name": "openrouter-chrome-extension",
  "version": "1.0.0",
  "description": "OpenRouter CSV Cost Analyzer Chrome Extension",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "generate-icons": "magick icon-pwa.png -resize 16x16 -quality 100 src/extension/icons/icon16.png && magick icon-pwa.png -resize 32x32 -quality 100 src/extension/icons/icon32.png && magick icon-pwa.png -resize 48x48 -quality 100 src/extension/icons/icon48.png && magick icon-pwa.png -resize 128x128 -quality 100 src/extension/icons/icon128.png",
    "package": "bun run build && zip -r extension.zip dist/* && mv extension.zip ~/Desktop",
    "wss": "bunx wss-ext-reloader --port 3001",
    "dev:full": "concurrently --kill-others-on-fail \"bun run wss\" \"bun run dev\"",
    "lint": "eslint src --ext .js,.jsx,.ts,.tsx --quiet --format=compact",
    "lint:fix": "eslint src --ext .js,.jsx,.ts,.tsx --fix --quiet --format=compact",
    "release": "bun run build && git add . && git commit -m \"Release $(cat package.json | grep version | head -1 | awk -F: '{ print $2 }' | sed 's/[\" ,]//g')\" && git push",
    "release:tag": "git tag v$(cat package.json | grep version | head -1 | awk -F: '{ print $2 }' | sed 's/[\" ,]//g') && git push --tags",
    "setup:api": "node scripts/setup-google-api.js",
    "generate-token": "node scripts/generate-refresh-token-from-json.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "chokidar": "^4.0.3",
    "chokidar-cli": "^3.0.0",
    "chrome-cli": "^1.1.1",
    "concurrently": "^9.2.1",
    "eslint": "^8.0.0",
    "rollup-plugin-copy": "^3.5.0",
    "typescript": "5.3.3",
    "vite": "^7.1.5"
  },
  "keywords": [
    "chrome-extension",
    "openrouter",
    "csv-analyzer"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vite-plugin-extension-reloader": "^1.1.6"
  }
}
